apiVersion: metallb.io/v1beta1
kind: IPAddressPool
metadata:
  name: homelab-pool
  namespace: metallb-system
spec:
  addresses:
    # Conservative single-address pool matching the known live ingress IP.
    # Widen this range if additional LoadBalancer IPs are needed.
    - 192.168.141.230/32
