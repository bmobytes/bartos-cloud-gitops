{{- if .Values.nuqWorker.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: firecrawl-nuq-worker
  labels:
    {{- include "firecrawl.labels" . | nindent 4 }}
    app.kubernetes.io/name: firecrawl-nuq-worker
    app.kubernetes.io/component: nuq-worker
spec:
  replicas: {{ .Values.nuqWorker.replicaCount }}
  selector:
    matchLabels:
      app.kubernetes.io/name: firecrawl-nuq-worker
      app.kubernetes.io/part-of: firecrawl
  template:
    metadata:
      labels:
        app.kubernetes.io/name: firecrawl-nuq-worker
        app.kubernetes.io/component: nuq-worker
        {{- include "firecrawl.labels" . | nindent 8 }}
    spec:
      containers:
        - name: nuq-worker
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command: ["node", "--max-old-space-size=2048", "dist/src/services/worker/nuq-worker.js"]
          ports:
            - name: http
              containerPort: {{ .Values.nuqWorker.port }}
              protocol: TCP
          envFrom:
            - configMapRef:
                name: firecrawl-config
          env:
            - name: NUQ_DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: {{ include "firecrawl.pgSecretName" . }}
                  key: NUQ_DATABASE_URL
            - name: NUQ_DATABASE_URL_LISTEN
              valueFrom:
                secretKeyRef:
                  name: {{ include "firecrawl.pgSecretName" . }}
                  key: NUQ_DATABASE_URL_LISTEN
            - name: OPENAI_API_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ include "firecrawl.secretName" . }}
                  key: OPENAI_API_KEY
                  optional: true
          readinessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 30
            periodSeconds: 30
          resources:
            {{- toYaml .Values.nuqWorker.resources | nindent 12 }}
{{- end }}
