apiVersion: v1
kind: Service
metadata:
  name: honcho-postgres
  namespace: honcho
  labels:
    app.kubernetes.io/name: honcho-postgres
    app.kubernetes.io/part-of: honcho
spec:
  selector:
    app.kubernetes.io/name: honcho-postgres
  ports:
    - port: 5432
      targetPort: 5432
      name: postgres
