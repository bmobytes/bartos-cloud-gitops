apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: honcho
  namespace: honcho
  labels:
    app.kubernetes.io/name: honcho
    app.kubernetes.io/part-of: honcho
  annotations:
    cert-manager.io/cluster-issuer: lab-ca
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - honcho.lab.bartos.media
      secretName: honcho-tls
  rules:
    - host: honcho.lab.bartos.media
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: honcho
                port:
                  number: 80
